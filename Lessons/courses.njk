---
layout: layouts/base.njk
title: My Courses
permalink: /courses/
---

<main class="container" style="margin: 4rem auto; padding-bottom: 4rem;">
  <div style="text-align: center; margin-bottom: 4rem;">
    <h1 style="font-size: 3.5rem; font-weight: 900; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; margin-bottom: 1rem;">📚 My Courses</h1>
    <p style="font-size: 1.3rem; color: #718096; max-width: 700px; margin: 0 auto;">Explore all available programming courses and start learning today</p>
  </div>

  <div class="course-grid">
{% for course in collections.courses %}
    <div class="course-card">
      <h3><a href="{{ course.url }}">{% if course.data.courseId %}{{ course.data.courseId }}: {% endif %}{{ course.data.title or 'Untitled Course' }}</a></h3>
      <p style="flex-grow: 1; color: #4a5568; line-height: 1.7;">{{ course.data.description or course.data.excerpt or 'Explore this course and level up your skills...' }}</p>
      <div style="margin-top: 1.5rem; margin-bottom: 1.5rem;">
        {% if course.data.level %}
        <span class="tag">{{ course.data.level }}</span>
        {% endif %}
        {% if course.data.duration %}
        <span class="tag">⏱️ {{ course.data.duration }}</span>
        {% endif %}
      </div>
      <a href="{{ course.url }}" class="btn" style="margin-top: auto; text-align: center;">Explore Course →</a>
    </div>
{% endfor %}
  </div>

{% if collections.courses.length == 0 %}
  <div style="text-align: center; padding: 4rem; background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%); border-radius: 32px; box-shadow: 0 8px 30px rgba(0,0,0,0.08); max-width: 700px; margin: 0 auto;">
    <div style="font-size: 4rem; margin-bottom: 1rem;">🎓</div>
    <p style="font-size: 1.5rem; color: #1a202c; font-weight: 600; margin-bottom: 1rem;">No courses generated yet</p>
    <p style="color: #4a5568; margin-bottom: 2rem;">Run the Intelligent Photocopier to create your first course!</p>
    <a href="/" class="btn" style="font-size: 1.1rem; padding: 1.25rem 2.5rem;">Go to AI Course Builder →</a>
  </div>
{% else %}
  <div style="text-align: center; margin-top: 4rem; padding: 3rem; background: linear-gradient(135deg, #e0c3fc15 0%, #8ec5fc15 100%); border-radius: 32px;">
    <h3 style="font-size: 2rem; margin-bottom: 1rem;">🚀 Want to create more courses?</h3>
    <p style="color: #718096; font-size: 1.1rem; margin-bottom: 2rem;">Use our AI-powered course builder to generate custom courses</p>
    <a href="/" class="btn" style="font-size: 1.1rem; padding: 1rem 2.5rem; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">Create New Course →</a>
  </div>
{% endif %}
</main>
